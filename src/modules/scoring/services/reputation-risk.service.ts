import { Injectable } from "@nestjs/common";

/** Placeholder: reputation / brand-safety risk scoring. */
@Injectable()
export class ReputationRiskService {
  calculate(_input: unknown): number | null {
    return null;
  }
}
