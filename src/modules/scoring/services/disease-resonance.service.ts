import { Injectable } from "@nestjs/common";

/** Placeholder: disease / condition resonance scoring. */
@Injectable()
export class DiseaseResonanceService {
  calculate(_input: unknown): number | null {
    return null;
  }
}
