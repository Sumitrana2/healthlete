import { Injectable } from "@nestjs/common";

/** Placeholder: audience trust scoring. */
@Injectable()
export class AudienceTrustService {
  calculate(_input: unknown): number | null {
    return null;
  }
}
