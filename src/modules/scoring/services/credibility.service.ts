import { Injectable } from "@nestjs/common";

/** Placeholder: credibility score formulas. */
@Injectable()
export class CredibilityService {
  calculate(_input: unknown): number | null {
    return null;
  }
}
