import { Injectable } from "@nestjs/common";

/** Placeholder: final match-score aggregation. */
@Injectable()
export class MatchScoreService {
  calculate(_input: unknown): number | null {
    return null;
  }
}
